cxf.path: /order-service
server.port: 8092
spring.datasource.url: jdbc:mysql://localhost:3306/uber?pinGlobalTxToPhysicalConnection=true
spring.datasource.username: root
spring.datasource.password: kerin3989@K
spring.datasource.driver-class-name: com.mysql.cj.jdbc.Driver


jms.broker.url: tcp://localhost:61616

spring.jpa.hibernate.ddl-auto: update
spring.jpa.show-sql: true

spring:
  jpa:
    properties:
      hibernate:
        dialect=org:
          hibernate:
            dialect:
              MySQL5Dialect:
        format_sql: true

#  activemq:
#    broker-url: tcp://localhost:61616